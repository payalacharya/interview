apiVersion: v1
kind: Service
metadata:
  name: nginx-service-node-port
spec:
  selector:
    app: nginxF
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
